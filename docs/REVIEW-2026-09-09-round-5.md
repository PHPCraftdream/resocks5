# Ревью resocks5 — раунд 5, 2026-09-09

База проверки: `6c3f2bb8c933f209119888b9279b11c55fdb6bf9`. Проведён статический
просмотр входных SOCKS5/HTTP-протоколов, аутентификации, выбора маршрутов,
подключения через upstream, пула, recovery, фрагментации, туннелирования,
завершения сервера, конфигурации, манифестов и CI. Замечания сопоставлены с
четырьмя предыдущими отчётами и с исходниками зафиксированных зависимостей.

По условию этого раунда **тесты, сборки, линтеры, сканеры зависимостей,
бенчмарки и сетевые воспроизведения не запускались**. Сценарии ниже выведены
из кода; это не результаты выполненных тестов. Вклад оптимизаций в задержки
и расход памяти не измерен. Изменение этого раунда — только данный отчёт;
исходники, настройки и версии зависимостей не менялись.

Итог: **12 открытых пунктов: 8 P2 и 4 P3**. R5-09 и часть R5-11 продолжают
ранее описанные возможности оптимизации. P0/P1 в проверенном объёме не найдены.
Приоритет относится к описанному условию проявления, а не ко всем установкам
сервера. Ссылки на строки соответствуют базе проверки.

| Приоритет | Значение | Количество |
|---|---|---:|
| P0 | Массовый критический отказ или компрометация, требующие немедленного вмешательства | 0 |
| P1 | Существенное нарушение защиты или доступности с широким практическим воздействием | 0 |
| P2 | Функциональный дефект, нарушение протокола или обещанной гарантии при указанном условии | 8 |
| P3 | Локальный дефект API, избыточная работа или возможность уменьшить блокировки | 4 |

| ID | P | Замечание | Основное условие |
|---|---|---|---|
| R5-01 | P2 | Некорректный порт CONNECT расходует попытки и ухудшает рейтинг исправных прокси | Клиент прошёл auth либо разрешён anonymous; маршрут через upstream |
| R5-02 | P2 | Бюджет клиентского протокола начинается заново на следующих фазах | Медленный первый байт, handshake, при необходимости recovery |
| R5-03 | P2 | Idle timeout обрывает продолжающуюся отправку фрагментов ClientHello | Фрагментация длится дольше idle timeout при регулярной записи байтов |
| R5-04 | P2 | Уже закрытый запасной сокет превращается в отказ исправного upstream | Включён prewarm, peer закрыл spare раньше локального max age |
| R5-05 | P2 | SIGTERM не запускает предусмотренное завершение сервера | Остановка Linux-сервиса или контейнера стандартным сигналом |
| R5-06 | P2 | Пять секунд ожидания логгера не ограничивают остановку процесса | Зависшая блокирующая запись в файл, stdout или stderr |
| R5-07 | P2 | SOCKS5 принимает нулевую длину пароля и позволяет закрепить пустой пароль за init-пользователем | Известный включённый пользователь с hash = init |
| R5-08 | P2 | Допустимый opaque-байт в постороннем HTTP-заголовке ломает CONNECT | Значение заголовка содержит obs-text, не являющийся UTF-8 |
| R5-09 | P3 | Для малого бюджета попыток строится и сортируется весь список маршрутов | Большие списки прокси; несколько gate-кандидатов |
| R5-10 | P3 | Запись одного init-claim блокирует чтение пользователей при остальных входах | Конкуренция за users-файл либо медленное хранилище; промахи auth cache |
| R5-11 | P3 | Получение соединения форматирует лишние строки; IPv6 попадает в blocking resolver | Любой acquire; дополнительная работа для IPv6-литерала |
| R5-12 | P3 | Переполнение суммы весов делает публичный weighted_index детерминированным | Большие положительные конечные веса у внешнего вызывающего кода |

**R5-01 — P2 — Проверять authority CONNECT до выбора upstream и изменения рейтинга.**

[Парсер CONNECT](../crates/resocks5/src/http_proxy/handle_http_client.rs#L450)
проверяет у target только наличие двоеточия. Например, строка
`CONNECT example.com:notaport HTTP/1.1` с завершёнными заголовками проходит эту
проверку. При разрешённом anonymous или успешной аутентификации target поступает
в `establish_connection` без разбора порта.

Для SOCKS5-upstream сначала вызывается
[pool.acquire](../crates/resocks5-net/src/connect/connect_socks5_proxy.rs#L24),
затем выполняется greeting/auth; только после этого
[HostPort::parse](../crates/resocks5-net/src/connect/handshake_over_stream.rs#L62)
отвергает порт. [Учёт результата](../crates/resocks5/src/server/establish_connection.rs#L635)
считает это отказом прокси: `should_record_failure` исключает лишь `AtCapacity`.
Происходит переход к следующим маршрутам до исчерпания бюджета, а клиент
обычного HTTP-пути получает [502](../crates/resocks5/src/http_proxy/handle_http_client.rs#L201).
Клиентская ошибка вызывает ненужные соединения и штрафы исправным upstream.

CONNECT с пустым или недопустимым портом должен быть отвергнут самим сервером.
[RFC 9110, §9.3.6](https://www.rfc-editor.org/rfc/rfc9110.html#section-9.3.6).
Нужен строгий разбор authority до recovery, dial и рейтинга, с ответом 400
на неверный порт. Внутреннюю ошибку входного адреса также следует отличать
от отказа upstream. Критерий исправления: такие запросы не вызывают ни одного
подключения и не меняют веса; корректные domain/IPv4/bracketed-IPv6 продолжают работать.

**R5-02 — P2 — Один документированный бюджет превращается в несколько полных ожиданий.**

[NetworkConfig](../crates/resocks5/src/config/network_config.rs#L38) описывает
`client_protocol_timeout_sec` как бюджет от подключения до окончания setup
и начала forwarding. [Диспетчер](../crates/resocks5/src/server/handle_client.rs#L46)
тратит отдельный полный бюджет на `peek` первого байта. После него
[SOCKS5](../crates/resocks5/src/server/handle_socks5_client.rs#L39) и
[HTTP](../crates/resocks5/src/http_proxy/handle_http_client.rs#L88) создают новый
`timeout` той же длины. В recovery снова создаётся полный бюджет:
[SOCKS5](../crates/resocks5/src/server/handle_socks5_client.rs#L257),
[HTTP](../crates/resocks5/src/http_proxy/handle_http_client.rs#L289).

При бюджете T клиент может прислать первый байт непосредственно перед T,
завершить остальной handshake непосредственно перед 2T, а в recovery удерживать
неполный распознаваемый префикс почти до 3T. Обычный путь уже допускает около 2T
клиентского ожидания, помимо отдельного времени подключения к upstream.
Это конечные ограничения, но они слабее обещанного единого бюджета; клиентский
permit и обработчик остаются заняты дольше настройки.

Передавать абсолютный deadline клиентской фазы от места приёма соединения
и использовать оставшееся время в диспетчере, handshake и recovery. Область
отдельных upstream-таймаутов явно согласовать с описанием настройки.
Критерий исправления: расход времени на предыдущих клиентских фазах уменьшает
доступное время следующих фаз и не позволяет получить новый полный T.

**R5-03 — P2 — Таймер простоя измеряет длительность всей отправки ClientHello.**

[prepare_payload](../crates/resocks5/src/server/forward_tunnel.rs#L62) оборачивает
целый `send_possibly_fragmented` в `within_idle`. В пути накопления аналогично
ограничивается весь [step](../crates/resocks5/src/server/forward_tunnel.rs#L84),
включая последующую отправку. Сам
[цикл фрагментации](../crates/resocks5-net/src/connect/tls_fragment.rs#L114)
пишет и сбрасывает каждый chunk, затем ждёт `delay_ms`; успешная запись не
перезапускает окружающий таймер.

Например, при idle timeout 1 с, размере фрагмента 1 байт и задержке 100 мс
отправка полного ClientHello длиной более 100 байтов требует более 10 с.
Даже при немедленно успешных write/flush и движении байтов каждые 100 мс
forwarding прекращается примерно через 1 с. Это противоречит
[определению idle](../crates/resocks5/src/config/network_config.rs#L73).
Условие предполагает отключённый либо достаточно большой общий lifetime.

Учитывать фактический прогресс записи в стадии подготовки так же, как в основном
туннеле. Если нужен отдельный предел длительности фрагментации, дать ему отдельную
семантику. При истечении таймера сохранить терминальное закрытие обоих потоков:
уже отправленный префикс нельзя передавать повторно. Критерий исправления:
регулярная запись с интервалами меньше idle разрешена, действительно остановившаяся
запись завершается по idle. Это другой дефект, чем исправленный повтор префикса R3-02.

**R5-04 — P2 — Возраст spare не доказывает, что соединение пригодно для handshake.**

[checkout_prewarmed](../crates/resocks5-net/src/pool/proxy_pool.rs#L386) удаляет
только записи старше `max_session_age_sec`. Для остальных нет проверки уже
доступного EOF/ошибки. [acquire](../crates/resocks5-net/src/pool/proxy_pool.rs#L343)
немедленно возвращает такой stream и не выполняет fresh-connect fallback.

Upstream может закрыть соединение по своему таймауту ожидания greeting раньше,
чем истечёт локальный max age. Если FIN уже пришёл в очередь spare, следующий
handshake закончится на записи или чтении ответа. Между тем сервер upstream
может исправно принимать новые подключения. Маршрут уже занесён в
[tried_routes](../crates/resocks5/src/server/establish_connection.rs#L575), поэтому
при единственном обычном upstream клиент получает отказ без попытки открыть
к нему свежий сокет. Предварительное открытие соединений снижает доступность
по сравнению с отключённым пулом; сам пул описан как best-effort accelerator.

Отбрасывать сокеты с уже наблюдаемым закрытием/ошибкой и предусмотреть ограниченное
восстановление через fresh connect при отказе именно взятого spare до передачи
пользовательских данных. Учитывать permits и общий бюджет попыток, не вводить
бесконечный retry. Предварительная проверка сама по себе не устраняет гонку с FIN.
Критерий исправления: закрытый в очереди spare при доступном upstream не становится
окончательным отказом единственного маршрута.

**R5-05 — P2 — Обычная остановка контейнера обходит graceful shutdown.**

В [accept-loop](../crates/resocks5/src/server/run_server.rs#L187) обрабатывается
только `tokio::signal::ctrl_c()`. Закрытие listener и
[ожидание обработчиков до 30 с](../crates/resocks5/src/server/run_server.rs#L201)
происходят после выхода из этого цикла. Unix SIGTERM в эту ветку не входит.

В [Dockerfile](../Dockerfile#L64) указан прямой ENTRYPOINT, а переопределения
stop signal нет. Docker по умолчанию отправляет SIGTERM, затем SIGKILL после
истечения срока остановки; для Linux-контейнера стандартный срок составляет
10 с. [Docker: container stop](https://docs.docker.com/reference/cli/docker/container/stop/).
Следовательно, обычная остановка контейнера не запускает предусмотренный
drain активных туннелей и логгера. Точное действие необработанного сигнала
зависит также от запуска процесса как PID 1, но ветка приложения не исполняется
в обоих случаях.

На Unix включить SIGTERM в общий механизм завершения вместе с Ctrl+C.
Согласовать deployment stop timeout с 30 с ожидания обработчиков и 5 с ожидания
логгера. Критерий исправления: SIGTERM прекращает accept, даёт действующим
обработчикам завершиться в пределах установленного срока и доходит до log drain.

**R5-06 — P2 — Таймаут ожидания JoinHandle не ограничивает runtime teardown.**

[shutdown_log_drain](../crates/resocks5/src/main.rs#L480) передаёт `JoinHandle`
по значению в пятисекундный `timeout`. При истечении времени handle уничтожается,
а задача остаётся отделённой. Затем завершается функция с
[#[tokio::main]](../crates/resocks5/src/main.rs#L145), которая уничтожает runtime.

[Логгер](../crates/resocks5/src/main.rs#L418) использует `tokio::fs` и Tokio
stdout/stderr. В исходниках закреплённого Tokio 1.43.1 `io::Stdout` содержит
`Blocking<std::io::Stdout>`, а `io/blocking.rs::poll_write` отправляет операцию
в blocking pool. Запущенные blocking-операции продолжаются после отмены
async-задачи. `Runtime::drop` ожидает их завершения без ограничения; это указано
в `runtime/runtime.rs` и [документации Runtime](https://docs.rs/tokio/latest/tokio/runtime/struct.Runtime.html#shutdown).

Если получатель stdout-пайпа перестал читать либо зависла запись на хранилище,
пятисекундный срок не гарантирует выхода процесса. Дополнительно timeout-ветка
сама вызывает синхронный `eprintln!`, который может остановиться на stderr.
Это оставшаяся проблема границы shutdown после исправления наблюдаемости R25.

Ограничить завершение всего механизма вывода, включая runtime и blocking backend;
рассмотреть явно управляемый runtime с `shutdown_timeout` и соответствующей
политикой потери незаписанных сообщений. Простой `abort()` log-drain недостаточен
для уже запущенного blocking I/O; диагностический fallback тоже должен учитывать
зависшее назначение. Критерий исправления — ограниченное завершение процесса
при неотвечающем log sink, а не только завершение future ожидания логгера.

**R5-07 — P2 — Нулевой PLEN проходит RFC 1929 auth и сохраняет пустой пароль.**

[SOCKS5 auth](../crates/resocks5/src/server/handle_socks5_client.rs#L444) принимает
ULEN и PLEN как любые значения u8, включая ноль. Чтение пустого password-буфера
успешно, UTF-8-преобразование возвращает пустую строку, после чего вызывается
`verify_async`. RFC 1929 задаёт длины имени и пароля от 1 до 255 байтов.
[RFC 1929, §2](https://www.rfc-editor.org/rfc/rfc1929.html#section-2).

Для включённого пользователя с `hash = "init"`
[try_claim_init](../crates/resocks5/src/auth/state.rs#L191) передаёт пустую строку
в [compute_hash](../crates/resocks5/src/auth/compute_hash.rs#L10) и при успешной
записи users-файла закрепляет полученный Argon2 hash. Проверки непустого пароля
в этом пути нет. При этом [CLI](../crates/resocks5/src/cli/run_user_command.rs#L269)
явно запрещает пустой пароль. Требуется знать имя доступного init-пользователя;
сам принцип назначения пароля первым вошедшим здесь не оспаривается.

Отвергать нулевые ULEN/PLEN до проверки учётных данных и ввести общую проверку
допустимого пароля перед claim, чтобы правило не зависело от входного протокола.
Критерий исправления: некорректный auth-frame не меняет users-файл и auth cache,
init остаётся незанятым; допустимые длины 1 и 255 по-прежнему обрабатываются.

**R5-08 — P2 — Парсер CONNECT требует UTF-8 от всего блока заголовков.**

[parse_http_connect](../crates/resocks5/src/http_proxy/handle_http_client.rs#L445)
вызывает `str::from_utf8` для всей секции до разделителя заголовков. Достаточно
одного байта `0xE9` в значении неиспользуемого `X-Opaque`, чтобы корректные
ASCII request line, Host и Proxy-Authorization не были разобраны. Функция
возвращает ошибку, а handler закрывает соединение без HTTP-ответа этой ветки.

HTTP допускает obs-text `0x80..0xFF` в значениях полей; RFC рекомендует
рассматривать такие значения как opaque-данные, когда нет более специальных
правил поля. [RFC 9110, §5.5](https://www.rfc-editor.org/rfc/rfc9110.html#section-5.5).
Это не случай бинарного тела: байт находится внутри допустимого значения
заголовка, который CONNECT-прокси не использует.

Разбирать границы строк и полей по байтам; проверять нужное кодирование только
у request line и реально используемых полей. Аналогичный подход уже применён
в recovery-парсере после R2-07, но не перенесён в этот входной парсер.
Критерий исправления: добавление допустимого opaque-заголовка не меняет
результат корректного CONNECT; запреты управляющих байтов в необходимых полях
сохраняются.

**R5-09 — P3 — Стоимость полного порядка не зависит от малого бюджета попыток.**

[Ratings::pick_order](../crates/resocks5-net/src/rating/mod.rs#L114) строит N
ключей под mutex, затем
[сортирует весь массив](../crates/resocks5-net/src/rating/select.rs#L53) за
`O(N log N)`. [ProxyRotator](../crates/resocks5-net/src/rotator/mod.rs#L314)
материализует все N `Arc<ProxyConfig>`. Основная ветка использует только нужный
префикс, часто один успешный маршрут, при стандартном бюджете до 10 попыток.

В [gate-цикле](../crates/resocks5/src/server/establish_connection.rs#L486)
сначала строится полный порядок G gates, а для очередного gate — новый полный
порядок прокси. Проверка оставшегося бюджета находится внутри уже построенного
порядка. Несколько рассмотренных gates могут повторить работу `O(N log N)`;
даже исчерпанный бюджет не всегда предотвращает следующую сортировку.
В [обычном пути](../crates/resocks5/src/server/establish_connection.rs#L571)
порядок также строится до проверки бюджета.

Это незавершённая часть R26. R3-10 уже вынес сортировку из mutex, и замечание
не отменяет это исправление. Остались O(N) построение ключей под блокировкой,
полная сортировка и материализация ненужного хвоста.

Проверять бюджет до построения порядка. Для ограниченного префикса рассмотреть
выбор K минимальных случайных ключей с частичной сортировкой либо ограниченной
кучей вместо сортировки всех N; сохранять распределение выбора без возвращения
и корректно учитывать пропуски уже испытанных маршрутов. Цель — уменьшить
работу при K существенно меньше N, например до `O(N + K log K)` для подхода
с разделением массива. Критерии: прежняя семантика вероятностей и отсутствие
полной сортировки при малом бюджете. Практический выигрыш требует измерений.

**R5-10 — P3 — Межпроцессное ожидание и файловая запись удерживают общий users RwLock.**

[try_claim_init](../crates/resocks5/src/auth/state.rs#L199) захватывает write guard
всего списка пользователей и сохраняет его на протяжении
[persist_claim](../crates/resocks5/src/auth/state.rs#L278). Внутри выполняются
межпроцессный lock, чтение/разбор файла и атомарная запись с синхронизацией.
Только ожидание [UsersFileLock](../crates/resocks5/src/config/users_file.rs#L48)
может занять до 10 с; к нему добавляется время хранилища.

Другие входы с промахом auth cache блокируются на
[users.read](../crates/resocks5/src/auth/state.rs#L169), даже если относятся
к давно настроенным пользователям. Работа выполняется в blocking pool, но
занимает ограниченные [verify_slots](../crates/resocks5/src/auth/state.rs#L135):
при нескольких таких входах общий лимит проверки паролей оказывается занят
ожиданием одного claim. Входы с попаданием в cache этот lock обходят.

Отделить сериализацию изменения/сохранения claims от короткой публикации нового
состояния пользователей. Обычному чтению готового hash не нужно ждать чужого
файлового I/O. При переработке сохранить повторную проверку победителя гонки,
приоритет изменения на диске, атомарность сохранения и rollback при отказе;
простое снятие guard перед текущим `persist_claim` этих свойств не гарантирует.
Критерий исправления: занятый внешним процессом users-lock задерживает claim,
но не чтение готового hash другого пользователя. Приоритет P3 учитывает,
что init-claim редок; это не утверждение о постоянной блокировке всех входов.

**R5-11 — P3 — Лишние endpoint-строки и blocking hop для IPv6-литералов.**

[acquire](../crates/resocks5-net/src/pool/proxy_pool.rs#L343) форматирует
диагностический endpoint до проверки spare, хотя на успешном пути эта строка
не используется. При fresh connect дополнительно создаётся `format!("{}:{}", ... )`.
Сходный строковый адрес используется
[refill-задачей](../crates/resocks5-net/src/pool/proxy_pool.rs#L471).

Host хранится без IPv6-скобок. Поэтому для `host = "::1"`, `port = 1080`
получается `::1:1080`, который не разбирается как `SocketAddr`. В закреплённом
Tokio 1.43.1 `net/addr.rs` реализация `ToSocketAddrs` для `str` после этой
неудачи копирует строку и вызывает системный resolver через `spawn_blocking`.
Реализация для `(&str, u16)` сначала разбирает IPv4/IPv6 непосредственно и
возвращает готовый адрес. Этот пункт касается лишней аллокации и смены потока;
отказ системного разрешения такого IPv6-адреса здесь не утверждается.

Готовить диагностический endpoint только в ветке ошибки и передавать
`(proxy.host.as_str(), proxy.port)` в TCP connect. Для domain системный resolver
по-прежнему нужен; для IP-литерала промежуточная строка и blocking hop устранимы.
Форматирование endpoint — оставшаяся часть R27; следствие для IPv6 уточнено
в этом раунде. Критерии: отсутствие этой аллокации при успешном spare checkout
и одинаковое соединение по domain/IPv4/IPv6 после замены представления адреса.

**R5-12 — P3 — Конечные веса переполняют сумму в weighted_index.**

Публичный [weighted_index](../crates/resocks5-net/src/rating/select.rs#L8)
складывает исходные f64 без масштабирования. Для
`[f64::MAX, f64::MAX]` каждый вес положительный и конечный, а total становится
`+infinity`. Ненулевой случайный draw даёт бесконечный dart, который остаётся
таким после вычитания обоих весов; нулевой draw даёт NaN. Ни один вариант
не проходит условие выбора, и fallback всегда возвращает последний индекс 1
вместо равновероятного выбора двух равных весов.

Это нарушение контракта публичного примитива при допустимых по его описанию
весах. Основной ротатор использует `weighted_order`, а внутренний
`Ratings::pick` получает ограниченные веса sand-модели; для этих серверных
путей описанное переполнение не установлено. Поэтому приоритет P3.

Нормировать конечные положительные веса на максимальный перед суммированием,
явно определить обработку недопустимых весов и сохранить нулевой fallback.
Критерий исправления: умножение набора положительных весов на общий конечный
масштаб, включая масштаб с переполняющейся исходной суммой, не меняет
распределение выбора. Исправление нулевого draw из R3-08 нужно сохранить.

**Состояние предыдущего раунда и границы вывода.**

R4-08 закрыт до этого ревью коммитом `6c3f2bb`: `Cargo.lock` теперь содержит
anyhow 1.0.103, bytes 1.11.1 и tokio 1.43.1. Они соответствуют исправленным
версиям трёх advisory, перечисленных в
[раунде 4](REVIEW-2026-09-09-round-4.md). Это подтверждение изменения lock-файла;
новое сканирование всей базы advisory в данном раунде не проводилось.

Не переоткрываются исправленные ограниченность sticky cache, индексы маршрутов,
экранирование логов и разбор opaque-байтов в recovery. Публичный `checkout`,
который возвращает bare TCP stream без учёта permit, документирован как отдельный
низкоуровневый путь; R5-04 относится к обычному `acquire` и пригодности spare.
Результаты запусков из прежних отчётов и сообщения предыдущего коммита не
считаются проверками этой ревизии, выполненными автором данного ревью.

Практический порядок исправления: валидация/auth (R5-01, R5-07, R5-08), границы
таймаутов и пригодность spare (R5-02–R5-04), завершение процесса (R5-05, R5-06),
затем локальный дефект выбора и оптимизации P3. Все 12 пунктов остаются
открытыми: отчёт фиксирует механизмы и критерии будущей проверки, а не исправления.
